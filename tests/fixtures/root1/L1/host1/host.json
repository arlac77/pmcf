{
  "priority": 11,
  "os": "linux",
  "depends": ["d1", "d2-${os}"],
  "replaces": ["r1", "r2-${os}"],
  "provides": ["p1", "p2-${os}"],
  "extends": [
    "/model/m1",
    "/model/${os}",
    "/services/timemachine",
    "/services/mosquitto"
  ],
  "networkInterfaces": {
    "eth0": {
      "kind": "ethernet",
      "network": "/L1/n1",
      "ipAddresses": "192.168.1.1"
    }
  },
  "services": {
    "mosquitto": {
      "alias": "mqtt",
      "persistence_location": "/var/lib/mosquitto",
      "password_file": "/etc/mosquitto/passwd",
      "acl_file": "/etc/mosquitto/acl"
    },
    "dns": { "type": "dns", "alias": "dns" },
    "openldap": {
      "alias": "ldap",
      "baseDN": "${ldap_base}",
      "rootDN": "dn=root,${ldap_base}",
      "uri": "ldap://"
    },
    "chrony": {
      "alias": "ntp",
      "source": "/GLOBAL"
    }
  }
}
